# Base URL of your backend API (no trailing slash). Example:
# VITE_API_BASE=https://api.example.com
# When running behind the same host with a proxy, you can leave the default '/api'.
VITE_API_BASE=/api
