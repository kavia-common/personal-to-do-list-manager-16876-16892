# Frontend environment configuration for Personal To‑Do (SvelteKit)

# PUBLIC_INTERFACE
# VITE_API_BASE sets the base path used by the frontend API client.
# In production, set this to where your backend is exposed.
# If your host proxies /api to the backend, you can leave it as /api.
VITE_API_BASE=/api
# Examples:
# VITE_API_BASE=https://api.example.com
# VITE_API_BASE=https://your-domain.com/api

# PUBLIC_INTERFACE
# VITE_DEV_PROXY_TARGET configures the Vite dev server to proxy /api
# requests to your backend during local development.
# Example if your backend runs at http://localhost:4000:
# VITE_DEV_PROXY_TARGET=http://localhost:4000
# Leave it unset if your backend is available at the same origin under /api.
# VITE_DEV_PROXY_TARGET=
